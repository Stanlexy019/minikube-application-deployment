apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: os-namespace
spec:
  selector:
    tier: api
  ports:
    - protocol: TCP
      port: 3500
      targetPort: 3500
  type: ClusterIP