apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: os-namespace
spec:
  selector:
    tier: db
  type: ClusterIP
  ports:
  - name: db-port
    protocol: TCP
    port: 27017
    targetPort: 27107
    
    